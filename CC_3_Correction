import numpy as np

params=(0,1)
x = 10*np.random.random
y = params[0] + params[1]*x + np.random.normal(size=len(x))

def moyenne(x) :
	for i in range(len(x)):
	  somme = somme + x[i]	
	  resultat = somme / len(x)
	return resultat
	return somme
